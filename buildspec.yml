version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
  build:
    commands:
      - cd frontend
      - npm ci
      - npm run build

artifacts:
  base-directory: frontend/dist
  files:
    - "**/*"
